"""Champion Cog modules whose maturity is curated (구현 완료·검증됨).

A module lives here only while its manifest entry declares
``CogMaturity.VERIFIED``; promotion or demotion must move the file and
update ``manifest.py`` together, enforced by
``tests/test_all_champion_modules.py``. No champion has reached this state
yet — every modeled Cog remains ``MODELED_UNVERIFIED`` pending client
measurement (see TASKS.md P0-041).
"""
