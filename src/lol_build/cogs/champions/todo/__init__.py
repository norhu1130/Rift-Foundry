"""Champion Cog modules whose maturity is todo (미구현).

A module lives here only while its manifest entry declares the
matching maturity; promotion or demotion must move the file and
update ``manifest.py`` together, enforced by
``tests/test_all_champion_modules.py``.
"""
